pipelines:
  - name: basic
    steps:
      - name: basic
        type: Bash
        configuration:
          runtime:
            type: host
#             image:
#               auto:
#                 language: javad
#                 versions:
#                   - master
        execution:
          onExecute:
            - echo 1
          
