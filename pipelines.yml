resources:
  - name: git
    type: GitRepo
    configuration:
      path: bharath92/sample-runSh
      gitProvider: github
      branches:
        include: master 

pipelines:
  - name: basic1
    steps:
      - name: first
        type: Bash
        configuration:
          runtime:
            type: host
          inputResources:
           - name: git
        execution:
          onExecute:
            - printenv
            - echo 1
      - name: second
        type: Bash
        configuration:
          runtime:
            type: host
          inputSteps:
            - name: first
        execution: 
          onExecute:
            - echo 1
