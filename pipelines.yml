resources:
  - name: dRes
    type: dummy
    configuration:
      foo: bar

pipelines:
  - name: basic
    steps:
      - name: basic
        type: Bash
        configuration:
          inputResources:
           - name: dRes
        execution:
          onExecute:
            - printenv
      - name: dStep
        type: dummy
        configuration:
          inputResources:
           - name: dRes
        execution:
          onExecute:
            - printenv

